# VolGuard Backend Requirements
# Python 3.11+

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36

# Data processing
pandas==2.2.3
numpy==1.26.4
pytz==2024.2

# HTTP clients
requests==2.32.3
aiohttp==3.10.10
httpx==0.27.2

# Upstox broker SDK (required)
upstox-python-sdk==2.19.0

# Volatility modelling
scipy==1.14.1
arch==7.1.0

# Intelligence layer (all optional — system works without them)
anthropic==0.40.0
groq==0.11.0
feedparser==6.0.11

# Macro data — replaces yfinance (blocked on AWS datacenter IPs)
# Twelve Data: free tier 800 calls/day — global equities, FX, commodities
# Free key at: https://twelvedata.com
# FRED: US 10Y yield from Federal Reserve. Free key at: https://fred.stlouisfed.org
# Both use the requests library already installed above — no extra packages needed.

# Utilities
python-dotenv==1.0.1

# NOTE: yfinance intentionally removed.
# Yahoo Finance blocks AWS/datacenter IPs reliably.
# Replaced with: Twelve Data (equities/FX/commodities) + FRED (bonds) + CoinGecko (crypto, no key)
